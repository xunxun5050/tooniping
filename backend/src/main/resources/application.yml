spring:
  application:
    name: webtoon-hub-backend
  datasource:
    url: jdbc:h2:file:./data/webtoon_hub;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always

server:
  port: 8080
